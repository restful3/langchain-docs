# 복사해서 .env 로 쓰세요:  cp .env.example .env
# 둘 다 비워 두면 email_agent.py 는 규칙 기반 시뮬레이션으로 돕니다(키 불필요).

# 분류·초안 생성을 실제 LLM 으로 하려면 (리포트가 쓴 모델: gpt-5-nano)
OPENAI_API_KEY=sk-...

# langgraph dev (§5) · LangSmith Studio 추적용
LANGSMITH_API_KEY=lsv2...
