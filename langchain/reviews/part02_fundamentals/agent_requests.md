# Part 2: LangChain 기초 에이전트 요청사항

> 작성일: 2026-03-01
> 기반: `decisions.md` 확정 사항 (2026-02-28 리뷰 회의)

---

## 사전 작업: 백업 생성

작업 시작 전 원본 파일을 백업하여, 수정 결과가 만족스럽지 않을 경우 즉시 원복할 수 있도록 한다.

```bash
cp docs/part02_fundamentals.md docs/part02_fundamentals.md.bak
cp src/part02_fundamentals/04_tools_advanced.py src/part02_fundamentals/04_tools_advanced.py.bak
cp src/part02_fundamentals/05_tool_calling.py src/part02_fundamentals/05_tool_calling.py.bak
```

> 모든 요청 완료 및 최종 확인 통과 후 백업 파일을 삭제한다. "최종 확인" 섹션 참조.

---

## 요청 1: Multimodal 섹션 대폭 축소

- **파일**: `docs/part02_fundamentals.md`
- **위치**: L712\~L998 (섹션 2.7 Multimodal Content Handling, 약 286줄)
- **현재**: 이미지(URL/Base64/File ID), PDF, 오디오, 비디오, 실전 Agent 예제, 프로바이더별 지원 포맷 테이블, 주의사항까지 포괄적으로 다룸
- **변경**:
  1. **유지**: L712\~L801 (Multimodal 개요, 지원 모델 확인, 이미지 URL 방식, 이미지 Base64 방식)
  2. **삭제**: L802\~L868 (PDF 문서 입력, 오디오 입력, 비디오 입력)
  3. **삭제**: L870\~L912 (실전 예제: 이미지 분석 Agent) — 이 예제는 deprecated `create_agent` API도 사용 중
  4. **축소**: L923\~L997 (주의사항/포맷 테이블) → 이미지 관련 주의사항만 간략히 남기고, 전체 포맷 비교 테이블은 공식 문서 링크로 대체
  5. **추가**: 축소된 섹션 말미에 "PDF, 오디오, 비디오 등 추가 멀티모달 입력은 [공식 문서](../official/08-messages_ko.md)를 참조하세요" 안내 문구 삽입
- **상세**: "소개를 하는 건 좋은데 좀 과하다", "프로바이더 특화 내용 다 빼버리고 LangChain 엔드유저용 인터페이스 기준으로만 설명하자" (회의 합의). 이미지 URL + Base64 두 가지 방식만 본문에 유지하면 핵심 개념 전달에 충분
- **예상 감축**: 약 150\~180줄 감소

### 검증 — 요청 1

- [ ] 축소 후 이미지 URL 방식 코드 블록이 온전한지 확인
- [ ] 축소 후 이미지 Base64 방식 코드 블록이 온전한지 확인
- [ ] 삭제된 PDF/오디오/비디오 참조가 남아있지 않은지 검색
- [ ] 공식 문서 참조 링크가 유효한지 확인
- [ ] "핵심 포인트" 박스(L993\~L997) 내용을 이미지 중심으로 수정

---

## 요청 2: ToolRuntime 섹션 대폭 축소

- **파일**: `docs/part02_fundamentals.md`
- **위치**: L1176\~L1492 (섹션 4. ToolRuntime - 고급 Tool 기능, 약 316줄)
- **현재**: ToolRuntime 개념, 5가지 Runtime 속성(state/context/store/stream_writer/tool_call_id), Type-Safe 패턴, 실전 활용 패턴 3종, 주의사항, 성능 고려사항까지 상세 설명
- **변경**:
  1. **유지**: L1176\~L1200 (4.1 ToolRuntime이란? — 개념 소개 + 기본 코드 예제)
  2. **축소**: L1202\~L1290 (4.2 Runtime 속성) → 5가지 속성을 표 한 개로 요약 (속성명, 용도, 한 줄 설명). 각 속성별 코드 블록 전부 삭제
  3. **삭제**: L1292\~L1393 (4.3 Type-Safe ToolRuntime, 4.4 실전 활용 패턴)
  4. **삭제**: L1394\~L1492 (4.5 주의사항, 4.6 성능 고려사항)
  5. **추가**: 축소된 섹션 말미에 "ToolRuntime의 상세 활용법은 Agent/MCP를 다루는 후속 파트에서 실습합니다" 안내 문구 삽입
- **상세**: "여기에 와야 되는지 잘 모르겠다", "MCP랑 묶어서 뒤에 빼는 게 좋지 않을까" (회의 합의). 초급 학습자가 Agent를 만들어본 적 없는 상태에서 Agent 상태 접근 코드를 만나면 혼란. 대응 소스 파일도 부재
- **예상 감축**: 약 250\~270줄 감소

### 검증 — 요청 2

- [ ] 축소 후 4.1 기본 코드 예제가 문법적으로 완전한지 확인
- [ ] Runtime 속성 요약 테이블의 5가지 항목이 누락 없는지 확인
- [ ] 후속 파트 안내 문구가 자연스러운지 확인
- [ ] 섹션 번호 연속성 유지 (4.1만 남거나, 4.1 + 요약으로 구성)

---

## 요청 3: Pydantic 스키마 섹션 축소

- **파일**: `docs/part02_fundamentals.md`
- **위치**: L1495\~L1618 (섹션 5. Tools 고급, 약 123줄)
- **현재**: Pydantic BaseModel 기본 스키마, Field Descriptions(검증 규칙 ge/le/min_length 등), 중첩 모델(Address/ContactInfo/CompanyInput) 상세 설명
- **변경**:
  1. **유지**: L1495\~L1531 (5.1 Pydantic 스키마 기본 — WeatherInput 예제)
  2. **축소**: L1533\~L1573 (5.2 Field Descriptions) → `Field(description=...)` 기본 사용법만 2\~3줄로 요약. 검증 규칙(ge/le/regex 등) 상세 표는 삭제
  3. **삭제**: L1575\~L1618 (5.3 중첩된 복잡한 입력 타입)
  4. **추가**: "독스트링과 타입힌트만으로도 대부분의 Tool은 잘 동작합니다. Pydantic 스키마는 복잡한 입력 검증이 필요할 때 사용하세요" 안내 문구 삽입
- **상세**: "파이덴틱 없어도 독스트링 잘 쓰고 타입힌트 잘 줬으면 잘 돌아간다", "수단일 뿐", "소개하는 정도로 괜찮을 것 같다" (회의 합의)
- **예상 감축**: 약 60\~70줄 감소

### 검증 — 요청 3

- [ ] 5.1 기본 예제(WeatherInput)가 온전한지 확인
- [ ] docstring/type hints 우선 안내 문구가 섹션 3(Tools 기초) 내용과 자연스럽게 연결되는지 확인
- [ ] 섹션 5 제목을 "Tools 고급 — Pydantic 스키마 소개" 등으로 경량화 여부 검토

---

## 요청 4: deprecated API 수정 (교안)

- **파일**: `docs/part02_fundamentals.md`
- **위치/변경**:

| 줄 | 현재 | 변경 |
| --- | --- | --- |
| L366 | `from langchain.agents import create_agent` | `from langgraph.prebuilt import create_react_agent` |
| L367 | `return create_agent(model=model, tools=[get_weather])` | `return create_react_agent(model=model, tools=[get_weather])` |
| L874 | `from langchain.agents import create_agent` | `from langgraph.prebuilt import create_react_agent` |
| L909 | `agent = create_agent(` | `agent = create_react_agent(` |

- **상세**: `langchain.agents.create_agent`는 LangChain 1.x에서 제거됨. LangGraph의 `create_react_agent`가 공식 대체 API. 단, **요청 1에서 L870\~L912(이미지 분석 Agent 예제)가 삭제되면 L874/L909 수정은 불필요**해짐 — 요청 1 삭제 범위와 중복 여부 확인 필요

### 검증 — 요청 4

- [ ] 교안 내 `from langchain.agents import` 패턴이 0건인지 grep 확인
- [ ] `create_react_agent` 사용 시 `langgraph` import가 코드 블록 상단에 포함되는지 확인
- [ ] 요청 1에서 L870\~L912가 삭제될 경우 L874/L909 수정은 스킵

---

## 요청 5: deprecated API 수정 (소스코드)

- **파일**: `src/part02_fundamentals/04_tools_advanced.py`
- **위치/변경**:

| 줄 | 현재 | 변경 |
| --- | --- | --- |
| L31 | `from pydantic import BaseModel, Field, validator` | `from pydantic import BaseModel, Field, field_validator` |
| L70 | `print(f"   {get_weather_advanced.args_schema.schema()}")` | `print(f"   {get_weather_advanced.args_schema.model_json_schema()}")` |
| L93\~L94 | `@validator('email')` / `def validate_email(cls, v):` | `@field_validator('email')` / `@classmethod` / `def validate_email(cls, v):` |

- **상세**: Pydantic v2에서 `@validator` → `@field_validator` + `@classmethod`, `.schema()` → `.model_json_schema()`. 참고로 `solutions/exercise_03.py`는 이미 Pydantic v2 패턴(`@field_validator`)을 사용 중이므로 `04_tools_advanced.py`와의 일관성 확보 필요

### 검증 — 요청 5

- [ ] `python -m py_compile src/part02_fundamentals/04_tools_advanced.py` 통과
- [ ] `python src/part02_fundamentals/04_tools_advanced.py` 실행 시 에러 없음
- [ ] 교안의 대응 코드 블록(L1499\~L1531 부근)에도 Pydantic v2 문법이 반영되었는지 확인
- [ ] `validator` 패턴이 소스코드 전체에서 0건인지 grep 확인: `grep -rn "@validator" src/part02_fundamentals/`

---

## 요청 6: 모델명 최신화 검증

- **파일**: `docs/part02_fundamentals.md`, `src/part02_fundamentals/*.py`
- **현재 상태** (사전 조사 결과):
  - Gemini: 이미 `gemini-2.5-flash-lite`, `Gemini 2.5 Pro/Flash`로 업데이트됨 (Gemini 1.5 참조 0건)
  - Anthropic: `claude-sonnet-4-5-20250929`, `claude-opus-4-5-20251101`, `claude-haiku-4-5-20251001` 사용 중
  - Azure: `gpt-4.1` (L200)
- **변경**:
  1. Anthropic 모델 ID를 [Anthropic 공식 모델 목록](https://docs.anthropic.com/en/docs/about-claude/models)과 대조하여 실존 여부 확인. 불일치 시 실제 ID로 수정
  2. Azure `gpt-4.1` (L200) — OpenAI/Azure 모델 목록과 대조하여 Azure 배포명으로 유효한지 확인. 불일치 시 `gpt-4o` 등 검증된 모델명으로 교체 또는 주석 추가
  3. 소스코드 `01_chat_models.py` L158의 `claude-haiku-4-5-20251001`, L159의 `gemini-2.5-flash-lite`도 동일 기준 검증
- **상세**: "이 자료가 굉장히 좀 아웃오브데이트 돼가지고" (참석자 2). Gemini는 이미 수정됨. Anthropic/Azure는 공식 문서와 대조 검증 필요

### 검증 — 요청 6

- [ ] 교안 내 모든 모델 ID를 추출하여 공식 문서와 1:1 대조
- [ ] `grep -n "claude-\|gemini-\|gpt-" docs/part02_fundamentals.md`로 전수 확인
- [ ] `grep -n "claude-\|gemini-\|gpt-" src/part02_fundamentals/*.py`로 전수 확인
- [ ] 존재하지 않는 모델 ID가 0건인지 확인

---

## 요청 7: eval() 보안 경고 추가

- **파일**: `src/part02_fundamentals/05_tool_calling.py`
- **위치**: L71\~L82 (`calculate` 함수)
- **현재**:

  ```python
  @tool
  def calculate(expression: str) -> str:
      """수학 계산을 수행합니다.
      Args:
          expression: 계산할 수식 (예: "2 + 2", "10 * 5")
      """
      try:
          result = eval(expression)
          return f"{expression} = {result}"
      except Exception as e:
          return f"계산 오류: {str(e)}"
  ```

- **변경** (택 1):
  - **방안 A** (최소 변경): `eval()` 호출 위에 경고 주석 추가

    ```python
    # 주의: eval()은 임의 코드 실행 위험이 있습니다.
    # 프로덕션에서는 ast.literal_eval() 또는 numexpr.evaluate()를 사용하세요.
    result = eval(expression)
    ```

  - **방안 B** (안전한 대안 교체): `eval()` → 제한된 평가 함수로 교체

    ```python
    import ast
    result = ast.literal_eval(expression)  # 리터럴만 허용
    ```

    또는 `numexpr` 사용 (단, 추가 의존성)
- **권장**: 교육 목적의 예제이므로 **방안 A** 채택. 학습자가 패턴을 그대로 프로덕션에 복사하는 것을 방지하면서도, Tool Calling 흐름 설명의 단순성 유지
- **상세**: 태영 리뷰에서 지적. 학습자가 `eval()` 패턴을 그대로 복사할 위험

### 검증 — 요청 7

- [ ] 경고 주석이 `eval()` 직전에 위치하는지 확인
- [ ] 교안(docs)에서 `calculate` 함수를 인용하는 부분이 있으면 동일 경고 반영
- [ ] `grep -rn "eval(" src/part02_fundamentals/`로 다른 eval 사용이 없는지 확인

---

## 요청 8: 셋업 가이드 가시성 향상

- **파일**: `docs/part02_fundamentals.md`
- **위치**: L1\~L7 (문서 상단 메타 정보 영역)
- **현재**:

  ```markdown
  > 📚 **학습 시간**: 약 3-4시간
  > 🎯 **난이도**: ⭐⭐☆☆☆ (초급)
  > 📖 **공식 문서**: [07-models.md](...), [08-messages.md](...), [09-tools.md](...)
  > 💻 **예제 코드**: [part02_fundamentals 디렉토리](../src/part02_fundamentals/)
  ```

- **변경**: 메타 정보 블록에 셋업 가이드 링크 추가

  ```markdown
  > 📚 **학습 시간**: 약 3-4시간
  > 🎯 **난이도**: ⭐⭐☆☆☆ (초급)
  > 🛠️ **환경 설정**: [SETUP_GUIDE.md](../SETUP_GUIDE.md) — API 키 설정, 패키지 설치, 실행 환경 구성
  > 📖 **공식 문서**: [07-models.md](...), [08-messages.md](...), [09-tools.md](...)
  > 💻 **예제 코드**: [part02_fundamentals 디렉토리](../src/part02_fundamentals/)
  ```

- **상세**: 참석자 2가 env 로딩, API 키 설정, 실행 환경 문제로 초반에 막혔음. 셋업 가이드가 존재하지만 눈에 띄지 않는 상태. `SETUP_GUIDE.md` 경로는 실제 파일 위치와 대조 필요

### 검증 — 요청 8

- [ ] `SETUP_GUIDE.md` 파일이 실제로 존재하는 경로 확인 (`ls docs/SETUP_GUIDE.md` 또는 `ls SETUP_GUIDE.md`)
- [ ] 링크 상대 경로가 올바른지 확인
- [ ] 다른 파트 교안에도 동일 패턴 적용 여부 검토 (본 요청은 Part 2만 대상)

---

## 요청 9: 교안 라인 참조 / 파일명 최종 검증

- **파일**: `docs/part02_fundamentals.md`
- **현재 상태** (사전 조사 결과): decisions.md에서 지적한 6건의 라인 참조 오류와 2건의 파일명 오류가 **현재 교안에서는 모두 올바른 값으로 수정되어 있음**

| 교안 줄 | 참조 내용 | 실제 소스코드 | 상태 |
| --- | --- | --- | --- |
| L105 | `01_chat_models.py 라인 39-55` | `example_1_basic_chat()` L39\~L56 | 정상 |
| L205 | `01_chat_models.py 라인 129-158` | `example_5_multiple_providers()` L129\~L158 | 정상 |
| L269 | `01_chat_models.py 라인 81-101` | `example_3_temperature()` L81\~L101 | 정상 |
| L1061 | `03_tools_basic.py 라인 32-51` | `example_1_simple_tool()` L32\~L51 | 정상 |
| L1172 | `03_tools_basic.py 라인 58-84` | `example_2_tool_with_params()` L58\~L84 | 정상 |
| L1531 | `04_tools_advanced.py 라인 40-58` | `WeatherInput` + `get_weather_advanced` L40\~L58 | 정상 |
| L1618 | `04_tools_advanced.py 라인 301-340` | 중첩 Pydantic 모델 L301\~L340 | 정상 |

- **변경**: 추가 수정 불필요. 다만, **요청 1\~3에서 섹션 축소 후 라인 번호가 변동**되므로 축소 작업 완료 후 모든 `> 💻 **예제 코드**:` 라인 참조를 재검증해야 함
- **상세**: decisions.md 항목 5, 6에 해당. 리뷰 시점 이후 이미 수정됨

### 검증 — 요청 9

- [ ] 요청 1\~3 축소 완료 후, `grep -n "예제 코드" docs/part02_fundamentals.md`로 모든 라인 참조 추출
- [ ] 각 참조가 실제 소스코드 라인과 일치하는지 전수 대조
- [ ] 파일명이 실제 존재하는 파일과 일치하는지 확인

---

## 요청 10: 전체 분량 축소 목표 달성 확인

- **파일**: `docs/part02_fundamentals.md`
- **목표**: 현재 2,233줄 → **1,489줄 이하** (현재의 2/3)
- **예상 감축 내역**:

| 요청 | 대상 섹션 | 예상 감축 |
| --- | --- | --- |
| 요청 1 | Multimodal (L712\~L998) | 150\~180줄 |
| 요청 2 | ToolRuntime (L1176\~L1492) | 250\~270줄 |
| 요청 3 | Pydantic (L1495\~L1618) | 60\~70줄 |
| **합계** | | **460\~520줄** |

- **예상 결과**: 약 1,713\~1,773줄 (축소율 약 21\~23%)
- **참고**: 목표(2/3 이하 = 1,489줄)에는 미달할 수 있음. 추가 축소가 필요하면 다음을 검토:
  - 실습 과제(L1791\~L1902, 약 111줄) 정리
  - FAQ(L2001\~L2155, 약 154줄) 에서 중복 항목 제거
  - 실전 팁(L1903\~L2000, 약 97줄) 에서 중복 내용 축소
- **상세**: "너무 길다", "좀 다이어트를 시켜야 될 필요가 있다" (회의 전원 합의). 우선 핵심 3개 섹션 축소 후 줄 수 확인, 부족 시 추가 축소 범위 협의

### 검증 — 요청 10

- [ ] `wc -l docs/part02_fundamentals.md`로 최종 줄 수 확인
- [ ] 목표치(1,489줄) 달성 여부 판단
- [ ] 미달 시 추가 축소 대상 섹션 제안

---

## 작업 순서 권장

```text
[사전 작업] 백업 생성     ← cp *.bak
    ↓
요청 8 (셋업 가이드)     ← 단순 추가, 라인 변동 최소
    ↓
요청 4 (교안 deprecated API)  ← 삭제 전에 수정
    ↓
요청 7 (eval 보안 경고)  ← 소스코드 수정
요청 5 (소스 deprecated API)  ← 소스코드 수정
요청 6 (모델명 검증)     ← 교안+소스 검증
    ↓
요청 1 (Multimodal 축소)  ← 대량 삭제 (뒤→앞 순서로)
요청 2 (ToolRuntime 축소)
요청 3 (Pydantic 축소)
    ↓
요청 9 (라인 참조 재검증)  ← 축소 후 라인 번호 변동 반영
    ↓
요청 10 (분량 목표 확인)   ← 최종 줄 수 체크
    ↓
[최종 확인] 통과 시 백업 삭제  ← rm *.bak
```

**핵심 원칙**: 대량 삭제(요청 1\~3)는 뒤쪽 섹션부터 진행하여 앞쪽 라인 번호에 영향을 주지 않도록 한다. 삭제 전에 점 수정(요청 4\~8)을 먼저 완료한다.

---

## 최종 확인

- [ ] 수정된 교안 전체를 한 번 읽어 흐름이 자연스러운지 확인
- [ ] 수정된 예제 코드가 `python -m py_compile` 통과
- [ ] decisions.md의 확정 항목 10건이 빠짐없이 반영되었는지 대조
- [ ] git diff로 변경 사항 최종 리뷰
- [ ] 최종 줄 수가 목표(1,489줄 이하)에 도달했는지 확인
- [ ] 위 항목 모두 통과 시 백업 파일 삭제:

```bash
rm docs/part02_fundamentals.md.bak
rm src/part02_fundamentals/04_tools_advanced.py.bak
rm src/part02_fundamentals/05_tool_calling.py.bak
```

> 문제 발견 시 백업에서 원복: `cp docs/part02_fundamentals.md.bak docs/part02_fundamentals.md`
